// ---------------------------------------------------------------------------
// Baked-in layout overrides.
//
// This is the "committed to code" layer. The Visual Editor loads it as the base
// and merges any locally-saved tweaks (localStorage) on top. When you're happy
// with a layout, hit "Export" in Design Mode, copy the JSON, and paste it here —
// that makes the positions permanent for every visitor, not just your browser.
//
// Shape:  { "<editable-id>": { desktop: {...}, tablet: {...}, mobile: {...} } }
// ---------------------------------------------------------------------------

const LAYOUT_DEFAULTS = {}

export default LAYOUT_DEFAULTS
